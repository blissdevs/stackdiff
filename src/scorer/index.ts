/**
 * Scorer module for evaluating and comparing environment variable maps.
 * Provides utilities for scoring differences and formatting human-readable reports.
 */
export { scoreEnvMap, formatScoreReport } from "./envScorer";
export type { ScorerOptions, ScoreResult } from "./envScorer";
